---
name: scan
description: Run the PR prioritization CLI in dev mode with provided arguments
---

Run the CLI using tsx (no build required):

```bash
npm run scan:dev -- $ARGUMENTS
```

If no arguments are provided, run with `--help`.
